# Next Steps

1. Preserve or commit the existing dirty business work before broader shared-file integration.
2. Approve and provision a dedicated Codex service credential before BODEFM or shared-pilot configuration; never copy the development user's personal Codex session.
3. Pin the approved Codex runtime distribution; the documented Python package is unavailable from the configured index.
4. Add stale-run recovery and supervised worker startup; asynchronous submission, polling, timeout state and native interruption are operational locally.
5. Add explicit evidence inspection to the Chatbot UI; run-status APIs and progress are operational.
6. Add governed Performance tools measure by measure; the Revenue family is now reconciled and operational locally.
7. Add Admin worker-health history without shell or deployment actions.
8. Keep both flags disabled until the provider credential, RLS corpus and failure-recovery tests pass.
